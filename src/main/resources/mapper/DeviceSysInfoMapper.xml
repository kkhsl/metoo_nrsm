<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.metoo.nrsm.core.api.mapper.DeviceSysInfoMapper">

    <insert id="insert" parameterType="com.metoo.nrsm.core.api.dto.DeviceSysInfoDTO">
        INSERT INTO metoo_device_sys_info
        <trim prefix="(" suffixOverrides="," suffix=")">
            <if test="addTime != null">
                addTime,
            </if>
            <if test="updateTime != null">
                updateTime,
            </if>
            <if test="manufacturer != null and manufacturer != ''">
                manufacturer,
            </if>
            <if test="model != null and model != ''">
                model,
            </if>
            <if test="os != null and os != ''">
                os,
            </if>
            <if test="cpu != null and !cpu.isEmpty()">
                cpu,
            </if>
            <if test="macAddresses != null and !macAddresses.isEmpty()">
                macAddresses,
            </if>
        </trim>
        <trim prefix="VALUES(" suffixOverrides="," suffix=")">
            <if test="addTime != null">
                #{addTime},
            </if>
            <if test="updateTime != null">
                #{updateTime},
            </if>
            <if test="manufacturer != null and manufacturer != ''">
                #{manufacturer},
            </if>
            <if test="model != null and model != ''">
                #{model},
            </if>
            <if test="os != null and os != ''">
                #{os},
            </if>
            <if test="cpu != null and !cpu.isEmpty()">
                #{cpu},
            </if>
            <if test="macAddresses != null and !macAddresses.isEmpty()">
                #{macAddresses},
            </if>
    </trim>
    </insert>
</mapper>
