<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metoo.nrsm.core.mapper.MacMapper">

    <insert id="save" parameterType="com.metoo.nrsm.entity.nspm.Mac">
        INSERT INTO metoo_mac
        <trim prefix="(" suffixOverrides="," suffix=")">
            <if test="addTime != null ">
                addTime,
            </if>
            <if test="mac != null and mac != '' ">
                mac,
            </if>
            <if test="port != null and port != '' ">
                port,
            </if>
            <if test="type != null and type != '' ">
                type,
            </if>
            <if test="deviceIp != null and deviceIp != '' ">
                deviceIp,
            </if>
            <if test="deviceName != null and deviceName != '' ">
                deviceName,
            </if>
            <if test="tag != null and tag != '' ">
                tag,
            </if>
            <if test="ip != null and ip != '' ">
                ip,
            </if>
            <if test="hostname != null and hostname != '' ">
                hostname,
            </if>
            <if test="remote_device != null and remote_device != '' ">
                remote_device,
            </if>
            <if test="remote_port != null and remote_port != '' ">
                remote_port,
            </if>
        </trim>
        <trim prefix="VALUES(" suffixOverrides="," suffix=")">
            <if test="addTime != null ">
                #{addTime},
            </if>
            <if test="mac != null and mac != '' ">
                #{mac},
            </if>
            <if test="port != null and port != '' ">
                #{port},
            </if>
            <if test="type != null and type != '' ">
                #{type},
            </if>
            <if test="deviceIp != null and deviceIp != '' ">
                #{deviceIp},
            </if>
            <if test="deviceName != null and deviceName != '' ">
                #{deviceName},
            </if>
            <if test="tag != null and tag != '' ">
                #{tag},
            </if>
            <if test="ip != null and ip != '' ">
                #{ip},
            </if>
            <if test="hostname != null and hostname != '' ">
                #{hostname},
            </if>
            <if test="remote_device != null and remote_device != '' ">
                #{remote_device},
            </if>
            <if test="remote_port != null and remote_port != '' ">
                #{remote_port},
            </if>
        </trim>
    </insert>

    <update id="truncateTable">
        TRUNCATE TABLE metoo_mac
    </update>


</mapper>